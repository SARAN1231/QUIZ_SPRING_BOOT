spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/quiz_db
    username: postgres
    password: saran3112
    driver-class-name: org.postgresql.Driver
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true

